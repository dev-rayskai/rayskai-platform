import Link from "next/link";import {PageHero,SiteShell} from "@/components/site-shell";import {articles} from "@/lib/articles";export default function Resources(){return <SiteShell><PageHero eyebrow="RESOURCES" title="Ideas for every conversation that matters." copy="Practical playbooks for teams that want to respond faster and turn more demand into growth."/><section id="playbooks" data-nav-label="Playbooks" className="mx-auto grid max-w-7xl gap-5 px-6 pb-28 lg:grid-cols-3">{articles.map(article=><article className="glass flex min-h-64 flex-col rounded-3xl p-7" key={article.slug}><p className="text-sm font-semibold text-primary">{article.category}</p><h2 className="mt-6 text-xl font-semibold">{article.title}</h2><p className="mt-4 text-sm leading-6 text-muted">{article.description}</p><Link href={`/blog/${article.slug}`} className="mt-auto pt-7 text-sm font-semibold text-primary">Read playbook →</Link></article>)}</section><div className="pb-28 text-center"><Link href="/blog" className="rounded-full border border-border px-5 py-3 text-sm font-semibold">View all resources</Link></div></SiteShell>}
