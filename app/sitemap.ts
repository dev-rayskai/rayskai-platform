import type {MetadataRoute}from "next";import {articles}from "@/lib/articles";export default function sitemap():MetadataRoute.Sitemap{const routes=['','/revenue-os','/voice-agent','/pricing','/integrations','/security','/customers','/resources','/blog','/contact','/privacy','/solutions/dental','/solutions/med-spa','/solutions/orthodontics','/solutions/primary-care','/solutions/specialty-clinics',...articles.map(article=>`/blog/${article.slug}`)];return routes.map(route=>({url:`https://rayskai.com${route}`,lastModified:new Date(),changeFrequency:'weekly',priority:route===''?1:.8}))}
