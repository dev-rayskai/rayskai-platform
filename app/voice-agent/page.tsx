"use client";import {motion} from "framer-motion";import {useState} from "react";import {PageHero,SiteShell} from "@/components/site-shell";const lines=["I’d like to book a cleaning.","Absolutely. I have Tuesday at 2:30 PM or Wednesday at 10:00 AM.","Wednesday at 10 works.","You’re all set. I’ll send a confirmation by text."];
export default function VoiceAgent(){const[step,setStep]=useState(0);return <SiteShell><PageHero eyebrow="AI VOICE AGENT" title="The call never goes unanswered." copy="A natural voice agent that answers, qualifies, books, and follows up."/><section className="mx-auto grid max-w-7xl items-center gap-12 px-6 pb-28 lg:grid-cols-2"><div><p className="eyebrow">Live AI call demo</p><h2 className="section-title mt-5">Hear one great answer in action.</h2><button onClick={()=>setStep((step+1)%lines.length)} className="mt-7 rounded-full bg-primary px-6 py-3.5 font-semibold text-white">▶ Listen to AI</button></div><motion.article key={step} initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} className="glass rounded-3xl p-7"><p className="text-sm text-primary">AI voice agent · 00:{18+step*8}</p><div className="my-10 flex h-20 items-center justify-center gap-1">{Array.from({length:24},(_,i)=><motion.i key={i} animate={{scaleY:[.4,1,.5]}} transition={{repeat:Infinity,duration:.8,delay:i*.04}} className="h-12 w-1 rounded-full bg-primary"/>)}</div><p className="max-w-[85%] rounded-2xl bg-primary p-4 text-white">{lines[step]}</p></motion.article></section></SiteShell>}
