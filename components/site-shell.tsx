import Link from "next/link";import {Navbar} from "@/components/navbar";import {SectionDots} from "@/components/section-dots";import {SiteFooter} from "@/components/site-footer";
export function SiteShell({children}:{children:React.ReactNode}){return <main><Navbar/><SectionDots/>{children}<SiteFooter/></main>}
export function PageHero({eyebrow,title,copy}:{eyebrow:string;title:string;copy:string}){return <section id="hero" data-nav-label="Overview" className="relative overflow-hidden px-6 pb-20 pt-40 text-center"><div className="grid-mask absolute inset-0 -z-20"/><div className="mx-auto max-w-3xl"><p className="eyebrow">{eyebrow}</p><h1 className="mt-6 text-5xl font-semibold tracking-[-.065em] sm:text-6xl">{title}</h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-8 text-muted">{copy}</p><Link href="/contact" className="mt-8 inline-flex rounded-full bg-primary px-6 py-3.5 font-semibold text-white">Book a demo</Link></div></section>}
